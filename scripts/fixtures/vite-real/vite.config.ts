import { defineConfig } from "vite";

// A genuinely real Vite app (installed + run for real in the scheduled/release
// lane). Vanilla single-server: the launcher passes its chosen port via the CLI
// flag, which is exactly the invocation app-it generates.
export default defineConfig({});
